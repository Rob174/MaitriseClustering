from setuptools import setup

setup(
  name = 'repr_partitions_cluster',
  packages = ['repr_partitions_cluster'], # this must be the same as the name above
  version = '0.1',
  description = 'Deep learning approach to predict which algorithm of first improvement or best improvement local search is better.',
  author = 'Robin M',
  author_email = 'robin.128.456@gmail.com',
  url = 'https://github.com/Rob174/MaitriseClustering',
  download_url = 'https://github.com/Rob174/MaitriseClustering/0.1',
  keywords = ['clustering', 'neural network' "recommender"], 
  classifiers = [],
  install_requires=[],
)